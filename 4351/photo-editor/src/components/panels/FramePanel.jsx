/** FramePanel.jsx — Frame/border controls (stub). */
import React from 'react';

export default function FramePanel() {
  return (
    <div style={{ padding: 'var(--space-8)', color: 'var(--color-text-secondary)', textAlign: 'center' }}>
      Frame controls — coming in Build 2.
    </div>
  );
}
